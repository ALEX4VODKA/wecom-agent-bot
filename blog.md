# 企业微信机器人里那串用户 ID，到底怎么变回用户名

最近在接企业微信智能机器人时遇到一个小问题：机器人收到消息以后，发送者有时是正常的企业 `userid`，有时却是一串较长的加密 ID。

一开始我以为只能在机器人侧继续查通讯录，但实际测试后发现，这条路并不合适。企业微信已经给了比较明确的对接方式：机器人负责收发消息，自建应用负责身份转换和通讯录查询。

最后跑通的流程是：

```text
Bot 收到消息
    ↓
from.userid
    ↓
自建应用调用 userid 转换接口
    ↓
企业内部 userid
    ↓
user/get
    ↓
姓名和部门
```

如果机器人本身返回的已经是明文 `userid`，则可以直接进入通讯录查询；如果返回的是加密用户标识，则先通过自建应用的 `access_token` 调用官方转换接口。

实现上有两个地方比较容易混淆。

第一，**Bot Secret 和自建应用 Secret 不是同一个东西**。Bot Secret 用来建立机器人长连接，自建应用 Secret 则用于获取 `access_token`、转换 userid 和读取通讯录。

第二，这并不是拿 Secret 在本地“解密”。程序只是把机器人返回的加密标识提交给企业微信，由企业微信服务端完成映射，再返回企业内部 userid。

我最后把这部分单独做成了一个身份解析层：

```python
identity = await resolver.resolve(raw_userid)

userid = identity.userid
name = identity.name
```

后面的工单、告警等逻辑都只使用标准化后的 `userid` 和 `name`，不再关心机器人最开始返回的是明文还是密文。

这次排查给我的一个经验是，遇到第三方平台的身份字段时，最好先确认平台有没有官方的 ID 转换机制，不要一开始就自己猜字段格式或设计映射表。实际把官方接口跑通后，后面的代码反而比较简单。

> 公开示例代码中已去除真实 CorpID、Bot ID、Secret、userid、姓名、部门和内部网络信息。
